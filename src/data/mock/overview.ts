import type { TrafficOverview } from '../types'

export const mockOverview: TrafficOverview = {
  currentPV: 13200,
  previousPV: 15000,
  currentUsers: 4950,
  previousUsers: 4800,
  pvChangePct: -0.12,
  usersChangePct: 0.03125,
}
